# Contributing

This is a personal portfolio. PRs that fix bugs or accessibility are welcome.

1. Fork and branch from `main`.
2. Keep the editorial chrome — no widget piles, no chess, no heatmap cards.
3. Swap real content in `lib/content.ts` rather than hard-coding names in layouts.
4. Run `npm run lint` and `npm run build` before opening a PR.
